import { AddCornersDirective } from './add-corners.directive';

describe('AddCornersDirective', () => {
  it('should create an instance', () => {
    const directive = new AddCornersDirective();
    expect(directive).toBeTruthy();
  });
});
