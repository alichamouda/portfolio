import { SetBackgroundColor } from './set-background-color.directive';

describe('GetColorDirective', () => {
  it('should create an instance', () => {
    const directive = new SetBackgroundColor();
    expect(directive).toBeTruthy();
  });
});
